# General Architecture

## ECS (Entity Component System)
The engine is built around the Entity Component System (ECS) architecture, which allows for flexible and efficient management of game objects. In this system, entities are composed of components that define their behavior and properties. Systems operate on these components to update the game state.
